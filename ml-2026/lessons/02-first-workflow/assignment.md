# 第 02 章练习：完整工作流复现与错例检查

预计 60 分钟。入口：[`02-first-workflow.ipynb`](notebook/02-first-workflow.ipynb)。数字来自 Notebook 已执行输出，可直接核对；任务 2 需要运行错例定位三行。

## 任务

1. **复现实验并填全记录表（30 分）**：填写小结页的记录表——模型（基线多数类、Logistic(C=1)）×（验证准确率、测试准确率、测试错例数）三列，每个分数注明“和谁比、在哪测、数字哪来”。只读路径可从已执行输出抄录，注明“未本机重跑”。
2. **检查至少 3 个错例（40 分）**：用下面的三行定位测试集错例，逐条记录真实类别、预测类别，并区分观察与猜测。观察＝图上直接看到的（真实是几、预测成几）；猜测＝你对写错原因的解释（如“这个 9 写得像 4”）——记录时标注哪句是猜测。不足 3 个就如实报告（本章冻结模型在测试集上的错例数是已知值）。

   ```python
   prediction = final_model.predict(X[test_idx])  # final_model：冻结后用 1437 条重训的模型
   wrong = np.flatnonzero(prediction != y[test_idx])
   print(len(wrong))
   ```

3. **AI 建议核验记录（20 分）**：记录一次你检查或否决 AI 建议的依据，格式：“我改了什么，因为哪个证据”，写在 Notebook 的文字格子里（第 1 章四步约定的第④步）。也可对照“审查 AI 生成的代码”一页的教学示例，写出你会在第②步审查里提出的两条问题。
4. **复现（10 分）**：随机种子 42、两次切分的次序、Python/scikit-learn 版本；说明基线在验证 37/360 与测试 36/360 的一分之差为什么不必过度解读。

## 提交与共同规则

提交运行过的 Notebook（保留输出）和 300–500 字说明，可直接写入 Notebook。重启内核后从头运行，记录 Python/依赖版本、随机种子、划分方式；外部素材保留来源链接。只改指定条件，其余保持一致。模型分数按实际运行填写，不以分数高低评分。

允许使用免费 AI 工具，也可完全不用 AI。使用者记录工具、日期、采纳或拒绝的一条建议以及人工验证方法；未使用者写“未使用”，附一条自己的代码检查记录。无需购买 API、GPU 或公开提交竞赛。课程提供的上游案例优先复用，说明修改之处即可。

本练习为形成性学习证据，上面的 100 分量规用于反馈，不直接新增总评权重。机器学习理论最终按任务/概念 20%、机制 25%、评估 35%、选型/解释 20% 汇总；机器学习项目另占总评 10%。
